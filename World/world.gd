extends Node3D

@onready var health_label: Label = $HUD/VBoxContainer/HealthLabel

#func _ready() -> void:
	#CarManager.health = CarManager.max_health

#
#func _process(_delta: float) -> void:
	#if Input.is_action_just_pressed("reset"):
		##CarManager.health = CarManager.max_health
		#get_tree().reload_current_scene()
		#
